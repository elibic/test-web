{
  "name": "functions",
  "description": "Cloud Functions — שכבת התראות תמיד-חיה למערכת מעלית שבת (test-94822)",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "test": "node test/night.test.js && node test/watch.test.js",
    "serve": "firebase emulators:start --only functions",
    "logs": "firebase functions:log",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "luxon": "^3.5.0",
    "nodemailer": "^6.9.16"
  }
}
